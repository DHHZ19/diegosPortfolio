---
export interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time class="p-1" datetime={date.toISOString()}>
  {
    date.toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
